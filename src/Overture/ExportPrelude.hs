module Overture.ExportPrelude ( module Prelude ) where

import Prelude hiding ( (.), id, const, fst, snd
                      , map, foldr, foldr1, foldl, foldl1
                      , drop, take, replicate, length, cycle, lookup, splitAt
                      , elem, notElem
                      , sum, product, and, or, all, any, maximum, minimum
                      , (++), concat, concatMap
                      , mapM, mapM_, sequence, sequence_
                      , head, tail, last, init, (!!)
                      , read
                      )

